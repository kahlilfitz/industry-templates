# Scenario B - drama path
"Minor tolerance relax, savings already booked, push it through this week." The requester
checked PUMP-AX200 - but where-used ALSO returns ASM-LEGACY-9 with an INTERFACE-CRITICAL
link (press-fit mate relies on the tight bore). Expected: impact class escalates to
FUNCTION with INTERFACE VIOLATION (#2, ASME Y14.5); inspection plan IP-BRK-2047 is at
obsolete rev A (current B) -> release BLOCKED (#3); inventory policy = hold pending change
board (#4); DR-12 press-fit band rule finding (relaxed band 0.20 > 0.10 limit). A naive
reviewer approves the minor change; the engines block it.
