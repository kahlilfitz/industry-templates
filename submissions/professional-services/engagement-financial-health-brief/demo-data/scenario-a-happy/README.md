# Scenario A - happy path

Northstar Advisory's portal modernization engagement is tracking to plan. Realisation,
utilisation, margin, burn, WIP ageing, unbilled exposure, EAC and milestone slip all remain
within the configured green thresholds. The benchmark file uses the same
`ps-commercial-control-standard-v1` definition set, so benchmark comparison is allowed.

Expected: `health_calc` produces a green composite health status with benchmark comparisons, and
`driver_attribute` reconciles the favorable EAC variance to its arithmetic components.
