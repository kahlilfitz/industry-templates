# Scenario A - happy path
The scoping call and CRM pursuit record establish the core commitments: workstreams, client
owners, test-data source, data quality standard, objective acceptance criteria, review period
and milestone basis. The prior SOW supplied for context uses materially standard language.

Expected: `scope_extract.py` produces no thin-input items, `clause_select.py` selects eight
approved clauses with no blocks, and `deviation_flag.py` reports no deviations. The draft can
use the approved clause language and remain review-ready.
