# Scenario A - clean scoped pre-check

Northbridge Advisory LLC asks for market-entry strategy support. The corporate extract resolves the submitted name to a controlled entity with no relevant family expansion beyond itself. The client, matter, adverse-party and independence register exports contain unrelated synthetic entities only.

Expected: `entity_resolve` resolves Northbridge with a controlled match; `conflict_classify` finds zero hits and produces `no_hits_found_subject_to_review`. The memo language must state the scope: registers searched, date range and entity variants tried. It must not say the matter is cleared.
