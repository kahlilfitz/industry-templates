# Scenario B — drama path

The obvious answer is wrong. The partner says the client is fine with public naming because the client's marketing team co-presented at a conference. The BD request proposes the descriptor "a top-five European insurer, 2026, core platform migration" if naming is blocked.

Expected: `permission_check` refuses naming because engagement terms expressly prohibit marketing use. The conference consent is event-only and expired. Logo use and named-individual testimonial use are refused. The 60% processing-time figure is structurally separated into `uncleared_figures` because it is a client-supplied internal estimate, while the verified workflow migration count remains cleared. The proposed anonymised descriptor is flagged as re-identifying and replaced with a safer descriptor.
