# My Industry Template

**Replace all of this.** This README becomes the main content on your template's gallery page,
so write it for the person deciding whether to install it — not for the agent.

Open with one paragraph: who this is for, and what it hands them.

## How it works

| # | Skill | Job |
|---|-------|-----|
| 1 | `example-intake` | Reads the messy inputs into the contract |
| 2 | `example-analyze` | Runs the deterministic engine |
| 3 | `example-draft` | Drafts the artifact a person will sign |

Say explicitly which parts are deterministic. "Numbers come from the engine; the model extracts
and drafts the cited prose" is the sentence that earns trust.

## The case that shows why it exists

Describe `demo-data/scenario-b-drama/` — the case where the obvious answer is wrong. Two or three
bullets: what invites the wrong call, and which facts in the documents overturn it. This is the
section a reviewer actually reads.

## What you bring

The documents and exports a user needs to have on hand. Be concrete about formats.

## Boundaries

What this does *not* do. Draft-first, no write-back, nothing auto-sent — and where the scenario
hands off to something else.

Grounded in <standards> — see `references/`.

---

Don't write "Skills in this package", "Demo data", "Install" or "Test evidence" sections. Those
are appended automatically from your actual payload.
