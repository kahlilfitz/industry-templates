Put your versioned contract here, e.g. `mfg.my-template.v1.json`.

One contract per package. Every skill reads and writes it, and every hop carries `data`,
`confidence`, `provenance` and `citations` so escalation is mechanical rather than a judgement
call. Version it in the filename — a breaking change is a new `.v2`.
