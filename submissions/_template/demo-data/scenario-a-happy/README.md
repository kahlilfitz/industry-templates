# Scenario A — happy path

The clean case. Describe the inputs and the expected outcome in three or four lines, and prove
the chain runs end to end.

Put the structured inputs in this folder, the messy originals in `raw/` (the email, the PDF
excerpt, the CSV export), and the committed engine outputs in `expected/`.
