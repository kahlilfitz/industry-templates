# Scenario B - drama path
"Operator error, retrain, close by Friday" - the pressure is in the supervisor email.
Pareto: operator_error has 4 rows BUT calibration_drift has 6 across THREE part numbers and
TWO machines. Even if operator_error ranked first, rule #3.4 rejects it (recurrence > 1).
Expected: root cause calibration_drift, systemic=true, why-chain to the missing recall gate;
PA REQUIRED across affected scope (#5); closure NOT ready (verification pending); escalation
quotes #3.4. A naive CAPA closes on retraining; the engine will not.
